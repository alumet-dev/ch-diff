// Opaque pointer to struct.
typedef struct opaque_struct* opaque_struct_ptr;

// Another one.
typedef struct foo_ foo;

// A third one.
struct opaque;

// Opaque pointer to enum.
typedef enum opaque_enum* opaque_enum_ptr;

// Opaque pointer to union.
typedef union opaque_union* opaque_union_ptr;
